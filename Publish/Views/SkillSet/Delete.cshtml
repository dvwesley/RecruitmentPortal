@model RecruitmentPortal.Models.SkillSet
@{
    ViewBag.Title = "Delete Skill Set";
}
<br />
<div class="row">
    <div class="col-md-10 col-xs-7">
        <h4>Delete this Skill Set?</h4>
    </div>
    <div class="col-md-2 col-xs-5 text-right">
        @Html.ActionLink("Back to List", "Index", "SkillSet", null, new { @class = "btn btn-sm btn-default" })
    </div>
</div>
<hr />
@using (var f = Html.Bootstrap().Begin(new Form()))
{
    @Html.AntiForgeryToken()    
    @f.FormGroup().DisplayFor(model => model.Name)
    @Html.Bootstrap().SubmitButton().Text("Delete").Class("btn btn-sm btn-default")
}
